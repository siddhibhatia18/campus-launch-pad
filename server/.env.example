# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database Configuration (for upcoming phases)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/campus-launch-pad?retryWrites=true&w=majority

# JWT Authentication (for upcoming phases)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d
